<h1>Коммунальные расходы</h1>

<table>
  <thead>
    <tr>
      <th>Ресурс</th>
      <th>Дата</th>
      <th>Объём</th>
      <th>Цена за единицу</th>
      <th>Сумма</th>
      <th>Действия</th>
    </tr>
  </thead>
  <tbody>
    <% @utility_readings.each do |reading| %>
      <tr>
        <td><%= reading.resource %></td>
        <td><%= reading.date %></td>
        <td><%= reading.value %></td>
        <td><%= reading.unit_price %></td>
        <td><%= reading.total_cost %></td>
        <td>
          <%= link_to "✏️", edit_utility_reading_path(reading) %>
          <%= button_to "🗑 Удалить", utility_reading_path(reading),
                        method: :delete,
                        data: { confirm: "Удалить запись?" },
                        form: { style: "display:inline" } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

